import styled from 'styled-components';

const Container = styled.div`
  form {
    padding: 20px;
    width: 320px;

    display: flex;
    flex-direction: column;

    img {
      width: 206px;
    }
  }
`;

export const Styles = {
  Container,
};
